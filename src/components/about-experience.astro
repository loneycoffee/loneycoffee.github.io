---
import AboutItem from "./shared/about-item.astro";
const { logo, dates, role, company, description, link } = Astro.props;
---

<AboutItem
  logo={logo}
  dates={dates}
  title={role}
  organization={company}
  description={description}
  link={link}
/>
