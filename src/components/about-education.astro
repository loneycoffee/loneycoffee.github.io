---
import AboutItem from "./shared/about-item.astro";
const { logo, dates, specialization, school, description, link } = Astro.props;
---

<AboutItem
  logo={logo}
  dates={dates}
  title={specialization}
  organization={school}
  description={description}
  link={link}
/>
